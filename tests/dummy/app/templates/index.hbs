{{docs-hero
  logo="ember"
  slimHeading="File"
  strongHeading="Upload"
  byline="A file upload framework for Ember apps."
}}

<div class="docs-container docs-md">
  <section class="max-w-md mx-auto pb-4">
    <div class="my-16 text-center">
      {{#link-to
        "docs"
        class="bg-grey-darkest hover:bg-black text-white py-2 px-4 no-underline rounded"
      }}
        Get started →
      {{/link-to}}
    </div>
  </section>

  <section class="max-w-md mx-auto">
    <div class="my-16 text-center">
      <h1 class="my-8">Demo</h1>
      {{#file-dropzone name="photos" autoClear=true class="demo-dropzone" as |dropzone queue|}}
        <div class="dropzone-upload-area upload {{if dropzone.active "active"}}">
          {{#if dropzone.supported}}
            <div class="emoji mb-16">
              {{#if dropzone.active}}
                {{#if dropzone.valid}}
                  ✨👽✨
                {{else}}
                  🐮
                {{/if}}
              {{else}}
                👽
              {{/if}}
            </div>
          {{/if}}

          <p>
            {{#if dropzone.supported}}
              Drag your proof or
            {{/if}}
            {{#file-upload
              name="photos"
              for="upload-photo2"
              multiple=true
              onfileadd=(action "uploadProof")
              accept="image/*,video/*,audio/*"
            }}
              <a>select them</a>
            {{/file-upload}}
            to upload.
          </p>

          {{#if queue.files.length}}
            Uploaded {{queue.uploaded.length}} / {{queue.files.length}} files. ({{queue.progress}}%)

            {{#each queue.files as |file|}}
              <br> :: {{file.name}} -- {{file.progress}}
            {{/each}}
         {{/if}}
        </div>
      {{/file-dropzone}}
    </div>


    <div class="my-16 text-center">
      <h1 class="my-8">Demo</h1>
      {{#file-dropzone name="secondFileUploader" autoClear=false class="demo-dropzone" as |dropzoneX queueX|}}
        <div class="dropzone-upload-area upload {{if dropzoneX.active "active"}}">
          {{#if dropzoneX.supported}}
            <div class="emoji mb-16">
              {{#if dropzoneX.active}}
                {{#if dropzoneX.valid}}
                  ✨👽✨
                {{else}}
                  🐮
                {{/if}}
              {{else}}
                👽
              {{/if}}
            </div>
          {{/if}}

          <p>
            {{#if dropzoneX.supported}}
              Drag your proof or
            {{/if}}
            {{#file-upload
              name="secondFileUploader"
              for="upload-photo3"
              multiple=true
              onfileadd=(action "uploadProof")
              accept="image/*,video/*,audio/*"
            }}
              <a>select them</a>
            {{/file-upload}}
            to upload.
          </p>

          {{#if queueX.files.length}}
            Uploaded {{queueX.uploaded.length}} / {{queueX.files.length}} files. ({{queueX.progress}}%)

             {{#each queueX.files as |file|}}
              <br> :: {{file.name}} -- {{file.progress}}
            {{/each}}
          {{/if}}
        </div>
      {{/file-dropzone}}
    </div>
    {{!-- <div class="my-16">
      <ul class="demo-uploaded-files-list">
        {{#each model as |file|}}
          <li>
            <div class="card text-center">
              {{#if file.file}}
                <div class="mb-4">{{file.file.progress}}%</div>
              {{else if (equals file.type "image")}}
                <img src={{file.preview}} alt={{file.filename}}>
              {{else if (equals file.type "video")}}
                <video src={{file.preview}} alt={{file.filename}} muted loop autoplay>
                </video>
              {{/if}}
              <span class="caption">{{file.filename}}</span>
            </div>
          </li>
        {{/each}}
      </ul>
    </div> --}}
  </section>

  {{!-- master queue --}}
  <div style="border: 1px solid green">
     {{#each fileQueue.files as |file|}}
     <br><br>{{file.name}} -- {{file.queue.name}}
      {{/each}}
  </div>
</div>